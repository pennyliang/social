#include "social_db.h"

social_db::social_db(){};
social_db::~social_db(){};
/*bool social_db::build_db(  const char* import_file,
                                const char* key_file,
                                const char* data_file,
                                uint64_t record_count_of_corpus_file,
                                bool sorted)
{
	return true;
};
bool social_db::insert_db( const char* key_file,
                                const char* data_file,
                                uint64_t& suc_insert_cnt,
                                uint64_t& fail_insert_cnt,
                                uint64_t record_count_of_corpus_file,
                                bool is_truncate)
{
	return true;
};
*/
