#ifndef _SOCIAL_DB_H_
#define _SOCIAL_DB_H_
#include "thuirdb.h"
#include <iostream>
#include "string.h"
#include <stdlib.h>
#include "stdint.h"
#include <fstream>
class social_db
{

	public:
		social_db();
		~social_db();
		////////////////////////innerid 2 outerid/////////////////////////////////////
		/*bool build_db(	const char* import_file, 
				const char* key_file, 
				const char* data_file,
				uint64_t record_count_of_corpus_file,
				bool sorted);

		bool insert_db(	const char* key_file, 
				const char* data_file, 
				uint64_t& suc_insert_cnt, 
				uint64_t& fail_insert_cnt,
				uint64_t record_count_of_corpus_file,
				bool is_truncate);
		*/
};
#endif

